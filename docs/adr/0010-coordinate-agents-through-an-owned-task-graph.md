# Coordinate Agents through an owned Task graph

Agent Teams will coordinate through an explicit dependency graph in which every Task has one current Owner and all state transitions and Agent communications enter the Event Ledger. Agents keep independent Threads and return structured Completion Capsules instead of sharing hidden prompt state, accepting more explicit coordination data to make recovery, budgeting, and accountability deterministic.
