# Never expand capability through Delegation

A child Agent receives only an explicit subset of its parent's Task scope, resource budget, and capabilities, and further Delegation requires separately granted authority. Credentials, approvals, and unrestricted workspace access never propagate implicitly, trading convenience for a monotonic security boundary across the Agent tree.
