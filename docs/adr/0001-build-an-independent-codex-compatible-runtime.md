# Build an independent Codex-compatible runtime

GreenTyper will be an independent product that implements selected Codex-compatible protocol and Agent semantics, not a drop-in replacement for Codex CLI. This preserves freedom to make Agent Teams, Context Checkpoints, provider portability, and Windows-first behavior first-class while treating command, configuration, and TUI parity as selective compatibility rather than product identity.
